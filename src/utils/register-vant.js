import Vue from 'vue'

// 加载
import {
  Button,
  Cell,
  CellGroup,
  Icon,
  NavBar,
  Field
} from 'vant'

// 注册
Vue.use(Button)
  .use(Cell)
  .use(CellGroup)
  .use(Icon)
  .use(NavBar)
  .use(Field)
